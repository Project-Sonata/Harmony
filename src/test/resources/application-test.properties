eureka.client.enabled=false
spring.r2dbc.username=${TEST_DATABASE_USERNAME}
spring.r2dbc.password=${TEST_DATABASE_PASSWORD}
spring.r2dbc.url=${TEST_DATABASE_URL}
spring.flyway.user=${TEST_DATABASE_USERNAME}
spring.flyway.password=${TEST_DATABASE_PASSWORD}
spring.flyway.url=${TEST_MIGRATION_URL}


sonata.harmony.aws.credentials.key=${AWS_KEY}
sonata.harmony.aws.credentials.secretKey=${AWS_PASSWORD}

sonata.harmony.aws.buckets.imageBucketName=mafuyu-images1
sonata.harmony.aws.buckets.musicBucketName=music
sonata.harmony.aws.buckets.fallbackBucketName=default